# 食品类别分类系统 · 项目规范文档（SPEC）

> 版本：v1.0　|　依据：《现场服务工程师招标参数》（河南经贸，2026-07-15 最新版）标的 5-1
> 「基于机器学习的食品类别分类系统」　|　适用范围：本仓库前端与整体交付
> 本文档为前端开发的权威规范，后续所有界面改动均以本文为准。

---

## 1. 项目定位

- **类型**：轻量化 B/S 架构 AI 实训演示平台（Django 基础架构）。
- **目标用户**：高职学生（实训 / 课程演示 / 入门实操）、管理员（实训管理）。
- **教学目标**：帮助学生掌握「文本采集 → 模型推理 → 品类分类 → 数据统计」的完整机器学习基础流程。
- **难度定位**：适配高职实训难度，侧重基础功能演示与算法入门，**不要求工业级适配**。
- **交付性质**：提供完整源码、数据集、模型文件、部署手册，支持学生二次修改、参数调试、功能拓展。

---

## 2. 功能需求（权威清单，源自标的 5-1 第三节）

### 2.1 前端演示界面（学生端）
| 编号 | 功能 | 规范要求 |
|------|------|----------|
| ① | 基础账号登录 | 账号密码登录；区分学生 / 管理员权限 |
| ② | 文本采集上传 | 支持输入 / 上传食品名称与品类描述（文本采集） |
| ③ | 智能分类演示 | 一键 AI 推理，实时展示**分类结果、三级类目路径、置信度分值** |
| ④ | 基础数据展示 | 统计分类总数、各级类目占比，展示基础实训数据 |

### 2.2 PC 简易管理后台（管理员端）
| 编号 | 功能 | 规范要求 |
|------|------|----------|
| ① | 基础品类管理 | 食品品类新增、删除、基础信息编辑（三级类目体系） |
| ② | 样本与模型管理 | 样本管理、模型简易切换、模型基础准确率查看 |
| ③ | 记录查询统计 | 查看全部分类实训记录、简单汇总、**实训记录导出（CSV）** |
| ④ | 基础权限管理 | 简单账号管理、操作日志记录 |

### 2.3 扩展演示功能
- 支持简易模型迭代：学生自主扩充样本、微调模型参数，练习基础调优流程。
- 预留简易数据导出接口，支持实训数据本地保存，便于课程复盘。

---

## 3. 技术与运行环境约束（不可突破）

| 层 | 规范 | 说明 |
|----|------|------|
| 前端 | HTML / CSS / JavaScript + **Bootstrap** | 工程明确要求「基础 Bootstrap 样式」，不得引入重型前端框架（React/Vue 等） |
| 后端 | Django 轻量化框架 | 现有实现已满足 |
| 数据库 | MySQL 简易数据表（当前为 SQLite，可切换） | 实训演示可用 SQLite，但文档要求 MySQL 适配 |
| 算法 | SVM / 随机森林 / 简易 CNN（入门） | 当前为 TF-IDF + SVM / 随机森林 |
| 部署 | Windows 本地 / 简易 Linux 实训服务器 | 浏览器即可运行，无需工控设备 |
| 性能 | 普通实训电脑、低配流畅、无高算力要求 | 不得引入重资源依赖 |

> **前端硬约束**：必须基于 Bootstrap（CDN 或本地均可），保持「轻量化、易上手、易修改」的实训属性。视觉升级只能在此框架内做主题化与组件精细化，**不得改为 SPA 或引入构建工具链**。

---

## 4. UI / UX 设计规范

### 4.1 整体风格
- **调性**：克制、现代、教学友好。参考 emilkowalski（Vercel/Linear 审美）的「中性配色 + 单一低饱和强调色 + 柔阴影 + 大圆角 + 充足留白」。
- **双主题**：浅色为主，提供暗色切换（右上角按钮，localStorage 记忆，默认跟随系统）。
- **字体**：系统字体栈，含中文（PingFang SC / 微软雅黑）；标题紧致字距，正文行高 ≥ 1.6。

### 4.2 布局规范
- **顶栏**：极简 sticky 顶栏（非 Bootstrap 默认 navbar-dark），含品牌标识、当前用户、角色徽章、主题切换按钮。
- **容器**：内容限宽居中（container），主区域与顶栏 / 页脚留白充足。
- **后台导航**：管理后台五大模块（品类 / 样本 / 模型 / 记录 / 权限）需有**清晰的一级导航入口**（当前为按钮组，规范建议升级为侧边栏或顶部 Tab，提升可发现性）。

### 4.3 组件规范（基于 Bootstrap，主题化覆写）
- **卡片**：大圆角（≥16px）、柔和多层阴影替代实线边框；hover 轻微抬升。
- **按钮**：圆角（≥10px）；主操作使用单一 accent 色；次级操作为描边 / 幽灵按钮。
- **表单**：输入框 / 下拉聚焦时使用 accent 色描边 + 柔光，而非默认蓝。
- **表格**：行 hover 高亮；代码 / 编码用柔和等宽样式。
- **徽章**：pill 形态，柔和底色 + 对应文字色（替代刺眼 bg-success）。
- **进度条**：圆角、渐变 accent（用于置信度展示）。
- **提示**：alert 改为柔和左侧色条；关键操作反馈用轻量 Toast 弹出。

### 4.4 动效规范（克制，符合 emilkowalski 原则）
- 入场：卡片 fade + translateY(8px)，**ease-out**（非 ease-in），可交错延迟。
- 交互：按钮 hover / active 微缩放；尊重 `prefers-reduced-motion`。
- 禁止：夸张弹跳、过长动画、影响可读性的花哨效果。

### 4.5 响应式
- 桌面（≥992px）：演示页左右双栏（推理区 + 数据区）；后台卡片栅格。
- 平板 / 手机（<768px）：单列堆叠；顶栏用户信息与导航可折叠；表格允许横向滚动。

### 4.6 可访问性（基础）
- 表单 label 关联；按钮有可辨识文本；色彩对比度达标（WCAG AA）。
- 主题切换按钮有 aria-label。

---

## 5. 数据展示规范（对应功能 ③④ 与后台）

- **演示首页（④ 基础数据展示）** 必须呈现：一级 / 二级 / 三级类目数、标注样本数、累计推理次数、一级类目推理占比。
- **推理结果（③）** 必须呈现：预测三级类目名称 + 编码、类目路径、置信度（进度条）、所用模型、Top-3 候选。
- **后台仪表盘** 必须呈现：类目数 / 样本数 / 推理记录数 / 账号数 + 模型准确率 + 最近操作日志。

---

## 6. 完成物要求（标的 5-1 第五节，交付基线）

- [ ] 实训任务书 1 份
- [ ] 简易食品分类数据集 1 套（三级类目 ≥8/16/24，样本 ≥1000，标注准确率 ≥95%）
- [ ] 全套开源源码 1 份（本仓库）
- [ ] 基础模型评测数据 1 套
- [ ] 傻瓜式部署实训手册 1 份

> 前端工作须保证上述交付物中的「源码」「演示可用性」「部署手册截图」达标。

---

## 7. 前端现状 vs SPEC 差距（待完善项）

| 项 | 现状 | SPEC 要求 | 优先级 |
|----|------|-----------|--------|
| 整体风格 | 已做 emilkowalski 主题化（accent / 双主题 / 柔阴影 / reveal） | 达标 | 已完成 |
| 后台导航 | 按钮组（btn-group） | 升级为侧边栏 / 顶部 Tab，提升五模块可发现性 | **高** |
| 演示首页布局 | 双栏但信息密度一般，缺可视化 | 强化数据卡片 + 简易占比可视化 | **高** |
| 推理结果展示 | alert 块 | 改为结构化结果卡（路径 / 置信度环 / Top-3 列表） | **高** |
| 响应式 | 部分堆叠，表格未处理 | 表格横向滚动 + 顶栏折叠 | 中 |
| 可访问性 | 基础 | 补全 label/aria | 中 |
| 操作反馈 | 已有 Toast 组件 | 在增删改导出处实际接入 Toast | 中 |
| 模型管理交互 | 表单提交刷新 | 可加确认态 / 加载态（克制） | 低 |

---

## 8. 验收口径

- 学生端：登录 → 输入文本 → 一键推理 → 看到结构化结果与置信度 → 看到基础数据看板。
- 管理员端：五大模块均可通过清晰导航进入；增删改、记录导出可用；操作有日志。
- 视觉：浅 / 暗主题切换正常；桌面与移动端均可用；动效克制不干扰教学。
- 合规：仍基于 Bootstrap，无重型构建链，源码可直接在实训电脑运行。
