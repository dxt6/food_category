# 食品包装缺陷检测功能规范（SPEC）

> 版本：v1.0　|　状态：实施基线　|　更新日期：2026-08-04  
> 依据：《河南经贸现场工程师指标参数0715（最新版）》标的 5「基于深度学习的食品包装缺陷检测系统」及包装缺陷图像数据集指标。  
> 本文是后续 AI agent 修改视觉检测模块时的权威开发契约；如实现与本文冲突，以本文为准。

---

## 1. 产品目标

在现有 Django 食品类别分类系统中增加一套轻量化机器视觉实训流程，使学生可以完成：

`包装图片采集 → YOLO 缺陷推理 → 缺陷框与置信度查看 → 记录归档 → 课程复盘`

系统面向高职教学演示，不连接工业相机、PLC 或生产线，不宣称替代人工质检。实现必须保持代码易读、部署简单、普通浏览器可操作，并支持学生扩充样本后重新训练。

## 2. 招标指标映射

| 指标 | 系统实现 | 验收口径 |
| --- | --- | --- |
| 图片上传、摄像头拍照 | 学生端视觉检测页 | 本地文件和浏览器摄像头均可形成待检测图片 |
| 实时标注缺陷位置、类型、置信度 | YOLOv8n 推理与标注结果图 | 页面显示检测框列表、中文缺陷名、最高置信度与耗时 |
| 合格/不合格统计 | 视觉数据概览 | 显示累计检测、缺陷数、合格数与合格率 |
| 模型与样本管理 | 视觉模型管理页 + Django 命令 | 可审计数据、生成训练划分、训练并安装最佳权重 |
| 检测记录查询、筛选、导出 | 管理端检测记录页 | 支持按状态/结论筛选和 UTF-8 CSV 导出 |
| 操作日志 | `OperationLog` | 数据准备与导出写入操作日志 |
| 检测图片本地留存 | Django `MEDIA_ROOT` | 原图和标注图按日期归档 |

## 3. 数据事实与交付风险

### 3.1 权威数据源

- 根目录：`C:\Users\dongxiaotong\Desktop\教学资源开发\食品包装检测_数据集补充`
- 训练主数据：`images/`
- 原料与生成物：`external/`、`raw/`、`raw_off/`、`synthetic_barcode/`、`synthetic_defects/`
- 本模块不得读取顶层旧目录 `datasets/`、`datasets(1)/` 或 `智安食链 (2)/`。

### 3.2 已审计事实

- 图片：4056 张。
- YOLO 标签文件：1630 个。
- 原目录类别：`normal`、`stain`、`printing_error`、`seal_abnormal`、`bulge_leakage`。
- 有局部框的主要类别：表面污渍、印刷错误、胀气漏液。
- `seal_abnormal` 缺少局部框；`normal` 应作为无框负样本。
- 原 `data.yaml` 的训练集和验证集指向同一目录，不允许直接用于正式训练。

### 3.3 不得掩盖的差距

招标数据指标要求至少 6 类缺陷，当前仅有 4 类缺陷。包装破损、标签错位等类别尚未形成有效数据和完整框标注。AI agent 不得通过改名、复制图片或伪造类别声称已经达标；页面和数据报告必须保留该提醒。

## 4. 检测类别契约

YOLO 训练模型只包含缺陷框，不把 `normal` 训练为目标框类别。

| 训练 ID | 英文键 | 中文显示 | 原标签 ID | 标注策略 |
| ---: | --- | --- | ---: | --- |
| 0 | `stain` | 表面污渍 | 1 | 复用局部框 |
| 1 | `printing_error` | 印刷错误 | 2 | 复用局部框 |
| 2 | `seal_abnormal` | 封口异常 | 3 | 无框时生成整图弱标注 |
| 3 | `bulge_leakage` | 胀气漏液 | 4 | 复用局部框 |
| — | `normal` | 包装合格 | 0 | 空标签负样本，无检测框 |

推理结论规则：检测框数量大于 0 为“不合格”；没有超过阈值的检测框为“合格”。“合格”仅表示当前模型和阈值下未发现异常。

## 5. 数据准备契约

实现入口：`python manage.py prepare_vision_dataset`

1. 扫描 `images/<类别>/<材质>/` 下的 JPG、JPEG、PNG、BMP、WEBP。
2. 按“类别 + 材质”分组，用固定随机种子做 80%/20% 训练/验证划分。
3. 图片优先创建硬链接；跨磁盘或权限不允许时回退为复制。
4. 为所有图片创建标准 YOLO 标签文件：正常图为空文件；已有框重映射为 0–3；缺框异常图生成 `0.5 0.5 0.96 0.96` 整图弱标注。
5. 仅在训练侧通过硬链接复制少数缺陷类样本，使四类缺陷图数量与训练侧最大缺陷类对齐；验证集不得复制或污染。
6. 输出 `data.yaml`、`manifest.json` 与 `.vision-prepared` 安全标记。
7. `--force` 只允许删除带 `.vision-prepared` 标记的生成目录，绝不能修改原始数据。
8. 同一随机种子和源数据必须得到相同划分。

输出目录：`classifier/vision/prepared_dataset/`，不得提交到 Git。

## 6. 模型训练契约

实现入口：

```powershell
.venv\Scripts\python.exe manage.py train_vision --epochs 30 --imgsz 640 --batch 8 --device auto
```

- 模型：Ultralytics YOLOv8n，默认使用预训练 `yolov8n.pt`。
- `--device auto`：CUDA 可用时选 GPU 0，否则回退 CPU。
- 训练过程输出到 `classifier/vision/runs/`。
- 最佳权重安装到 `classifier/vision/models/food_package_yolov8n.pt`。
- 训练元数据写入 `food_package_yolov8n.meta.json`，至少包含时间、轮次、图像尺寸、batch、设备、数据配置、运行目录和评测指标。
- 网页请求中不得同步启动长时间训练；管理页只提供数据准备动作、模型状态和可复制训练命令。

依赖必须使用用户指定的 uv 与国内镜像安装：

```powershell
C:\Users\dongxiaotong\.local\bin\uv.exe pip install --python .venv\Scripts\python.exe --index-url https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
```

## 7. 后端接口与数据模型

### 7.1 路由（名称不可随意修改）

| URL | URL name | 权限 | 用途 |
| --- | --- | --- | --- |
| `/vision/` | `vision_demo` | 登录用户 | 图片采集、推理、结果与概览 |
| `/admin-panel/vision/` | `vision_record_list` | 管理员 | 检测记录筛选与复查 |
| `/admin-panel/vision/export/` | `vision_record_export` | 管理员 | 导出 CSV |
| `/admin-panel/vision/model/` | `vision_model_manager` | 管理员 | 数据审计与模型状态 |
| `/admin-panel/vision/prepare/` | `vision_prepare_dataset` | 管理员 POST | 生成标准训练集 |

### 7.2 `VisionDetectionRecord`

必需字段：原图、标注图、状态、是否缺陷、主要缺陷、最高置信度、检测框 JSON、推理耗时、模型名、阈值、错误信息、操作人、创建时间。

检测框 JSON 单项格式：

```json
{
  "class_id": 0,
  "class_name": "stain",
  "display_name": "表面污渍",
  "confidence": 0.91,
  "confidence_pct": 91.0,
  "box": [10.0, 12.0, 260.0, 188.0]
}
```

### 7.3 推理函数

`classifier.vision.pipeline.run_detection(image_path, output_path, confidence)` 必须：

- 延迟加载并缓存模型，Django 启动时不得立即加载 PyTorch 权重。
- 模型缺失时返回可理解的中文错误，不导致服务崩溃。
- 输出标注后的 JPG。
- 返回 `has_defect`、主要类别、置信度、检测框列表、推理耗时和模型名。

## 8. 前端信息架构

### 8.1 学生端

- 顶部导航保留“文本分类”和“包装检测”两个一级入口。
- 检测页桌面端为主工作区 + 数据侧栏，移动端单列。
- 采集区支持拖放、文件选择、`getUserMedia` 摄像头和拍摄预览。
- 阈值提供 15%、25%、40%、60% 四档，25% 为默认推荐。
- 结果必须同时展示：合格/不合格、主要缺陷、标注图、检测框数量、最高置信度、阈值、耗时和每个框的位置。
- 页面必须长期显示弱标注说明和“不可替代人工复核”的边界。

### 8.2 管理端

- 侧边栏增加“缺陷检测记录”和“视觉模型管理”。
- 记录采用图片卡片，保留状态、缺陷名、置信度、模型、操作人和时间。
- 数据审计页展示图像总数、局部框图片数、现有缺陷类、缺陷样本占比、训练/验证数与弱标注数。

## 9. 视觉规范

- 继续使用现有 Bootstrap + `app.css`，禁止引入 React/Vue 和构建链。
- 复用现有颜色变量、卡片、按钮、浅/暗主题、响应式断点和 `prefers-reduced-motion`。
- 合格使用主绿色；缺陷/弱标注提醒使用琥珀色；失败使用危险红色。
- 图片区域使用深色中性背景与 `object-fit: contain`，不裁掉检测框。
- 所有上传控件、摄像头按钮、结果图和状态必须具备可读标签或替代文本。

## 10. 安全与性能

- 上传仅允许 JPG/JPEG/PNG/WEBP，单文件不超过 12 MB，并用 Pillow 验证真实图片格式。
- 媒体文件写入 `MEDIA_ROOT`，开发环境由 Django 提供；生产环境由 Web 服务器提供。
- 不接受用户传入任意模型路径、数据路径或命令行内容。
- CSV 使用 UTF-8 BOM，保证中文版 Excel 可直接打开。
- 模型延迟加载；单个请求只处理一张图片；不设计高并发工业服务。

## 11. 验收测试

- [ ] 学生登录后可打开 `/vision/`。
- [ ] 有效图片可完成推理并生成 `VisionDetectionRecord`。
- [ ] 非图片、超限图片、模型缺失均显示中文错误。
- [ ] 检测有框时显示不合格和框明细；无框时显示合格声明。
- [ ] 摄像头拒绝授权时可回退图片上传。
- [ ] 管理员可筛选记录、查看图片、导出 CSV。
- [ ] 数据准备后训练/验证目录不同，类别重映射正确，正常标签为空，弱标注可统计。
- [ ] 训练完成后最佳权重可由网页推理加载。
- [ ] 浅色、暗色、桌面和移动端均无明显布局溢出。
- [ ] `python manage.py check` 与 `python manage.py test classifier` 全部通过。

## 12. Agent 修改规则

1. 优先修复根因，不用前端假数据掩盖模型或数据缺失。
2. 不删除文本分类功能，不修改既有账号与权限语义。
3. 不改动原始 7.9 GB 数据，不把生成训练集、运行日志或上传媒体提交到 Git。
4. 不把弱标注描述成精确局部标注，不声称当前数据已经达到 6 类交付指标。
5. 新增字段必须有 Django migration；新增行为必须补测试。
6. 页面文案使用简体中文，模型内部键保持本规范中的英文键。
7. 修改完成后必须运行 Django 检查、测试和浏览器关键流程验收。
