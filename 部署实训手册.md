# 傻瓜式部署实训手册：食品类别分类系统

适用对象：零基础实训学生。按步骤操作即可在本机跑起来。

## 0. 环境准备
- 安装 Python 3.10+（勾选 "Add Python to PATH"）。
- 打开命令行（Windows 用 PowerShell / cmd），确认：
  ```
  python --version
  pip --version
  ```

## 1. 安装依赖
进入本工程目录（含 `manage.py` 的文件夹），执行：
```
pip install -r requirements.txt
```
> 国内网络慢可加镜像：`pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple`

## 2. 初始化数据库与数据
```
python manage.py migrate
python manage.py load_data
```
- `migrate` 创建数据表（默认 SQLite，文件为 `db.sqlite3`）。
- `load_data` 灌入三级食品类目（90 条）与 1419 条标注样本，并创建演示账号。

## 3. 训练分类模型
模型已随数据集生成时一并训练；如需重新训练：启动后进入"模型管理"点击"重新训练"，
或命令行执行：
```
python -c "from classifier.ml import classifier as cl; cl.train('svm'); cl.train('rf')"
```
训练结果保存在 `classifier/ml/models/`，含 `svm.joblib`、`rf.joblib` 及对应 `*.meta.json` 准确率文件。

## 4. 启动系统
```
python manage.py runserver 0.0.0.0:8000
```
浏览器打开：http://127.0.0.1:8000/

## 5. 演示账号
| 角色 | 用户名 | 密码 |
| --- | --- | --- |
| 实训学生 | student | student123 |
| 后台管理员 | admin | admin123 |

- 学生登录后进入**演示首页**：输入食品文本 → 点"一键 AI 推理" → 查看三级类目、路径与置信度，右侧展示基础数据统计。
- 管理员登录后进入**PC 管理后台**：品类管理、样本管理、模型管理、记录查询、权限管理。

## 6. 常见操作
- **切换模型**：后台"模型管理" → 选 SVM 或随机森林 → "设为默认"，演示端即用该模型推理。
- **扩充数据 / 迭代**：后台"样本管理"新增食品样本 → "模型管理"重新训练 → 观察准确率变化。
- **导出记录**：后台"记录查询" → "导出 CSV"。

## 7. 切换为 MySQL（可选，对标招标参数）
1. 安装并启动 MySQL，创建数据库 `food_classify`。
2. 安装驱动：`pip install mysqlclient`
3. 编辑 `foodclassify/settings.py`，将 `USE_MYSQL = False` 改为 `True`，填写用户名/密码。
4. 重新执行 `migrate` 与 `load_data` 即可，业务代码无需改动。

## 8. 目录结构
```
食品类别分类系统/
├── manage.py
├── requirements.txt
├── 实训任务书.md
├── 部署实训手册.md
├── 模型评测报告.md
├── foodclassify/            # Django 工程配置
└── classifier/              # 业务应用
    ├── models.py            # 类目/样本/记录/用户/日志 等模型
    ├── views.py             # 演示端 + 后台视图
    ├── urls.py
    ├── templates/           # Bootstrap 前端页面
    ├── ml/
    │   ├── classifier.py    # TF-IDF + SVM/RF 训练与预测
    │   ├── generate_dataset.py  # 数据集生成器
    │   ├── dataset.json     # 三级食品分类数据集
    │   └── models/          # 训练好的模型与评测文件
    └── management/commands/load_data.py
```
